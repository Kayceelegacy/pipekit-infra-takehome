apiVersion: v1
kind: Service
metadata:
  name: postgrest
  namespace: postgrest
spec:
  type: NodePort
  selector:
    app: postgrest
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080
